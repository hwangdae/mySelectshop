# main.yml
name: main branch auto ci process script

on: # 아래 job을 실행시킬 상황
  push:
    branches: [ main ]

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest # 실행될 인스턴스 OS와 버전

    steps:
      # - name: Get Public IP
      #   id: ip
      #   uses: haythem/public-ip@v1.3

      # - name: Add EC2 Security Group Rule
      #   run: |
      #     echo "Public IP: ${{ steps.ip.outputs.ipv4 }}"
          
      #     aws ec2 authorize-security-group-ingress \
      #       --group-id ${{ secrets.AWS_SECURITY_GROUP_ID }}
      #       --protocol tcp \
      #       --port 22 \
      #       --cidr ${{ steps.ip.outputs.ipv4 }}/32

      - name: excuting remote ssh commands
        uses: appleboy/ssh-action@v0.1.6 # ssh 접속하는 오픈소스
        with:
          host: ${{ secrets.REMOTE_IP }} # 인스턴스 IP
          username: ${{ secrets.REMOTE_USER }} # 우분투 아이디
          key: ${{ secrets.REMOTE_PRIVATE_KEY }} # ec2 instance pem key
          port: ${{ secrets.REMOTE_SSH_PORT }} # 접속포트
          script: | # 실행할 스크립트
            cd mySelectshop
            git pull origin main
            /home/ubuntu/.nvm/versions/node/v22.14.0/bin/yarn install
            /home/***/.nvm/versions/node/v**/bin/yarn prisma generate
            /home/ubuntu/.nvm/versions/node/v22.14.0/bin/yarn build
            # pm2 start npm --name mySelectshop -- start
